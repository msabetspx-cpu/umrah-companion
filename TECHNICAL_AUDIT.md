# TECHNICAL_AUDIT — المراجعة التقنية

تاريخ: 2026-09-20. التقنية: HTML/CSS/JavaScript خالص (بدون أي framework أو build system — لم تتغيّر التقنية عمداً).

## 1) العمل دون إنترنت (Offline / PWA)
- **عامل الخدمة**: مُسجَّل في `app.js`، ويخزّن `./ , index.html, styles.css, app.js, manifest.json`.
- **إصدار الكاش**: رُفِع إلى `umrah-companion-v3`. عند التحديث: `install` يخزّن الجديد ثم `skipWaiting`، و`activate` يحذف الكاش القديم ثم `clients.claim` — فلا يمنع عامل الخدمة القديم ظهور الجديد.
- **بيانات المستخدم**: تُحفظ في `localStorage` (مفتاح `umrahState` + `theme`) ولا يمسحها تحديث الكاش إطلاقاً.
- **fallback**: عند فشل الشبكة لطلبات التنقّل تُعاد `index.html` المخزّنة.
- **لا اعتماديات خارجية**: لا CDN، لا Google Fonts، لا JS/CSS خارجي، لا API، لا صور خارجية، لا analytics. الأيقونة SVG مضمّنة، وfavicon مضمّن كـ data-URI. الخط `Amiri` تفضيل فقط يسقط لخطوط النظام دون طلب شبكة. روابط المصادر موسومة «تحتاج إنترنت» ولا يعتمد عليها عمل التطبيق.
- **تم اختباره فعلياً**: إيقاف الخادم + `context.setOffline(true)` ثم إعادة التحميل → التطبيق يفتح، والتنقّل والعدّاد يعملان (اختبارات B).

## 2) حفظ الحالة والترحيل
- **نطاق الحفظ: `sessionStorage` (تقدّم الرحلة مرتبط بالجلسة).** كل زائر/جلسة جديدة يبدأ من البداية (الاستعداد)، ويبقى العدد محفوظاً عند إعادة تحميل الصفحة داخل نفس الجلسة، ويُصفَّر عند إغلاق المتصفح وفتحه من جديد. يوجد زر «ابدأ العمرة من جديد» للتصفير اليدوي داخل الجلسة. الوضع الليلي وحده في `localStorage` (تفضيل مظهر دائم). وعند التحميل يُنظَّف أي تقدّم قديم كان في `localStorage` من إصدار سابق.
- يُحفظ في الجلسة: المرحلة، `completedTawaf`، `completedSai`، الـchecklist (بأقسامها)، معلومات الرحلة، الإعدادات (وضع الزحام).
- **الترحيل**: `loadState` يدمج المحفوظ مع الافتراضي (`Object.assign`) فلا تُفقد الحقول الجديدة، ويُرحّل `tawafCount/saiCount` القديمة إلى `completedTawaf/completedSai`.
- **الإصلاح الذاتي**: عند تلف `localStorage` يُلتقط الخطأ، تُستعاد القيم الافتراضية، وتُكتب حالة نظيفة (لا ينهار التطبيق) — تم اختباره.
- **تحقق من الحدود**: `clampCount` يمنع < 0 و > 7، والمرحلة غير الصالحة تُعاد إلى `PREPARATION`.

## 3) الأمان
- كل إدراج ديناميكي يمرّ بـ`escapeHtml`، والـchecklist تُبنى عبر DOM/`textContent`. **حمولة XSS في الإدخال لا تُنفَّذ** (تم اختباره).
- لا `eval`، لا حقن سكربت، لا أسرار. الروابط الخارجية `rel="noopener noreferrer"`.
- عامل الخدمة يخزّن ملفات محلية فقط (لا تسميم كاش من مصدر خارجي).

## 4) الأداء
- لا مكتبات؛ الحزمة كلها بضعة كيلوبايت. الحالة تُحفظ عند التغيير فقط، والقوائم تُبنى عند الحاجة. `body.crowd-mode` يُعطّل الحركات.

## 5) iPhone / الجوال
- `viewport-fit=cover` + `env(safe-area-inset-*)` للهيدر والشريط السفلي (النوتش/شريط الهوم).
- `min-height:100dvh` لتفادي مشكلة `100vh` في iOS.
- الزوم مسموح (لم نستخدم `user-scalable=no`). أهداف اللمس ≥ 48px. **لا تمرير أفقي عند 375px** (تم اختباره). وسوم `apple-mobile-web-app-*` للعرض المستقل.

## 6) ما لم أستطع التحقق منه
- **التثبيت الفعلي كـPWA على جهاز iPhone/Android حقيقي** (اختبرت التسجيل والكاش والعمل Offline في Chromium فقط، لا Safari فعلي).
- **قارئات الشاشة الفعلية** (روجعت سمات ARIA يدوياً).
- **دقّة النطق العربي** — يعتمد على أصوات المتصفح/النظام؛ لذلك سُمّي «قراءة صوتية» لا «تلاوة»، وليس الطريقة الوحيدة لعرض الذكر. (ملاحظة: في هذا الإصدار خُفّض الاعتماد على الصوت وأُزيلت أزرار النطق من الشاشات العملية إبقاءً للبساطة؛ النص هو الأساس.)

## 7) قائمة الاختبارات المنفّذة آلياً (Playwright / Chromium) — 47/47 ناجحة
راجع `tests/browser-test.mjs`. تشمل: التحميل بلا أخطاء، 11 مرحلة في «رافقني» مع next/prev، الطواف (1/7 حتى الإكمال، النقاط، السقف، التراجع، عدم السالب، الإعادة بتأكيد، الضغط السريع)، السعي والاتجاه، ثبات الحالة، تلف التخزين والإصلاح الذاتي، «ماذا أفعل الآن» السياقية، «اتلخبطت» والحالة غير الموثقة، البحث بالتطبيع العربي والرسالة الفارغة، فصل المصادر الشرعية عن الرسمية، صحة محتوى التحلل، أقسام الـchecklist وأمان XSS، معلومات الرحلة، وضع الزحام والوضع الليلي وثباتهما، غياب التمرير الأفقي عند 375px، تسجيل عامل الخدمة، والعمل Offline (خادم موقوف + شبكة مقطوعة) مع تنقّل وعدّاد.

### لم يُختبر آلياً (يحتاج جهازاً/بيئة حقيقية)
تثبيت PWA على iPhone/Safari، سلوك iOS standalone الفعلي، قارئات الشاشة، جودة النطق.
