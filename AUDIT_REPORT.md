# تقرير المراجعة الشاملة — تطبيق «رفيق العمرة»

مراجعة هندسية وأمنية وتجربة استخدام وعمل دون إنترنت، مع فحص المحتوى الشرعي.
تاريخ المراجعة: 2026-09-20.

> ملاحظة منهجية: لم أفترض أن أي شيء يعمل. اختبرت التطبيق فعلياً في متصفح Chromium
> عبر Playwright (28 اختباراً، جميعها ناجحة) بما فيها اختبار Offline حقيقي
> (إيقاف الخادم + قطع الشبكة ثم إعادة التحميل من الكاش).

---

## 1) Critical Issues (حرجة)

| # | المشكلة | الموقع |
|---|---|---|
| C1 | **عامل الخدمة لم يكن مُسجَّلاً إطلاقاً** — الملف `sw.js` موجود لكن لا يوجد أي `navigator.serviceWorker.register(...)`. النتيجة: التطبيق **لا يعمل Offline** رغم ادّعاء ذلك. | `index.html` / `app.js` |
| C2 | **تلف `localStorage` يعطّل التطبيق بالكامل** — `state = JSON.parse(saved)` بلا `try/catch`، وأي بيانات تالفة توقف كل شيفرة التهيئة. | `app.js:loadState` |
| C3 | **XSS مخزَّن في قائمة التجهيزات** — نص المستخدم يُدرج عبر `innerHTML` مباشرة (`${item.text}`)، فإدخال `<img onerror=...>` كان يُنفَّذ. | `app.js:renderChecklist` |

## 2) Major Issues (كبيرة)

| # | المشكلة | الموقع |
|---|---|---|
| M1 | **العدّاد يبدأ من 1 لا 0** ومنطقه ملتبس (يعرض «الشوط الحالي» بينما زر «أنهيت الشوط» يزيده)، مخالف للمطلوب 0→7. | عدّادات الطواف/السعي |
| M2 | **لا توجد آلة حالة** توجّه المستخدم عبر مراحل العمرة. | كامل التطبيق |
| M3 | **الضغط السريع المزدوج** يزيد العدّاد أكثر من مرة دون قصد. | العدّادات |
| M4 | **`loadState` يستبدل كائن الحالة بالكامل** فتُفقد الحقول الافتراضية الجديدة عند تحديث النسخة. | `app.js:loadState` |
| M5 | **لا يوجد fallback offline للتنقّل** في عامل الخدمة، ولا تنظيف للكاش القديم عند تغيير النسخة. | `sw.js` |
| M6 | **زرّا `view-ihram-rules` و«رافقني» بلا محتوى** — الأزرار موجودة لكن لا شاشة/منطق. | `index.html` |
| M7 | **لا نموذج بيانات موحّد للمحتوى الشرعي** (لا درجة حديث، لا رابط، لا `verifiedAt`، لا علم للخلاف). | `app.js` |

## 3) Minor Issues (صغيرة)

| # | المشكلة | الموقع |
|---|---|---|
| m1 | تعطيل التكبير/التصغير (`user-scalable=no`) يضر بسهولة الوصول. | `index.html` viewport |
| m2 | تباين ضعيف لبعض النصوص (`#7f8c8d` ثابت) وذهبي فاتح على أبيض. | `styles.css` |
| m3 | لا فحص فارغ لنتائج البحث (تظهر قائمة فارغة بلا رسالة). | `app.js` |
| m4 | `manifest`: `start_url` نسبي ملتبس، بلا `scope`/`id`، وأيقونة واحدة. | `manifest.json` |
| m5 | لا `favicon` مضمّن → طلب شبكة فاشل (`/favicon.ico`). | `index.html` |
| m6 | استخدام `e.target` في التنقّل قد يلتقط عنصراً داخلياً بدل الزر. | `app.js:setupNavigation` |
| m7 | لا تمييز للزر النشِط في الشريط السفلي. | UX |

---

## 4) Fixed Issues (جدول الإصلاح)

| Issue | Severity | Location | Problem | Fix | Status |
|---|---|---|---|---|---|
| C1 | Critical | `app.js` | عامل الخدمة غير مسجَّل | أُضيفت `registerServiceWorker()` تُسجّل `sw.js` عند التحميل | ✅ ثابت ومُختبَر Offline |
| C2 | Critical | `app.js` | تلف التخزين يعطّل التطبيق | `loadState` داخل `try/catch` + دمج مع الحالة الافتراضية + تنظيف القيم | ✅ مُختبَر |
| C3 | Critical | `app.js` | XSS مخزَّن في checklist | بناء العناصر عبر DOM و`textContent` + دالة `escapeHtml` لكل إدراج ديناميكي | ✅ مُختبَر (لم يُنفَّذ الحمولة) |
| M1 | Major | العدّادات | بدء من 1 ومنطق ملتبس | عدّ الأشواط **المكتملة** 0→7 مع نصوص واضحة | ✅ مُختبَر |
| M2 | Major | التطبيق | لا آلة حالة | `STAGES` + `advanceStage` (لطيفة، لا تمنع التصفّح) + تلميح المرحلة | ✅ |
| M3 | Major | العدّادات | زيادة مزدوجة | حارس نقر `makeTapGuard(250ms)` | ✅ مُختبَر |
| M4 | Major | `loadState` | فقد الحقول الجديدة | `Object.assign(DEFAULT_STATE, saved)` + حقل `schema` | ✅ |
| M5 | Major | `sw.js` | لا fallback/لا تنظيف كاش | `activate` يحذف الكاش القديم + `navigate` fallback لـ`index.html` + `skipWaiting`/`clients.claim` | ✅ مُختبَر |
| M6 | Major | `index.html` | أزرار بلا محتوى | أُضيفت شاشة محظورات الإحرام ووضع «رافقني» متعدّد الخطوات | ✅ |
| M7 | Major | `app.js` | لا نموذج بيانات شرعي | نموذج موحّد: `source, sourceUrl, hadithNumber, grading, verifiedAt, showDisagreement, needsHumanReview, notes` | ✅ |
| m1 | Minor | viewport | تعطيل الزوم | أُزيل `user-scalable=no` | ✅ |
| m2 | Minor | CSS | تباين ضعيف | ألوان `--muted`/`--secondary` بتباين أعلى + حالات تركيز | ✅ |
| m3 | Minor | البحث | لا رسالة فراغ | رسالة «لا توجد نتائج» / «لم أجد إجابة موثقة» | ✅ مُختبَر |
| m4 | Minor | manifest | قصور PWA | `id`, `scope`, `start_url:"./"`, `lang/dir`, أيقونة maskable | ✅ |
| m5 | Minor | HTML | 404 favicon | favicon مضمّن كـ data-URI | ✅ مُختبَر |
| m6 | Minor | التنقّل | `e.target` | استبدل بـ`e.currentTarget` | ✅ |
| m7 | Minor | UX | لا زر نشِط | تمييز الزر النشِط في الشريط السفلي | ✅ |

---

## 5) Religious Content Issues
انظر ملف **`RELIGIOUS_AUDIT.md`**. الخلاصة: المحتوى الأصلي كان **مسؤولاً** (كل نص له مصدر، وتنبيه صريح لعدم وجود دعاء مخصوص لكل شوط — وهذا صحيح ومهم). الإصلاحات: هيكلة البيانات، إظهار درجة الحديث والخلاف الفقهي في الواجهة، وتحديد بند واحد يحتاج تحقق مصدره الدقيق (رقم أثر تقصير المرأة).

## 6) Offline / PWA Issues
- **قبل:** لا تسجيل لعامل الخدمة (لا يعمل Offline)، لا تنظيف كاش، لا fallback، manifest ناقص.
- **بعد:** يعمل Offline بالكامل (مُختبَر بإيقاف الخادم + قطع الشبكة)، تنظيف تلقائي للكاش القديم، fallback للتنقّل، manifest كامل قابل للتثبيت.
- **الاعتماديات الخارجية:** لا يوجد أي CDN أو Google Fonts أو استدعاء API. الخط `Amiri` مذكور كتفضيل فقط ويسقط تلقائياً لخطوط النظام **دون أي طلب شبكة**. روابط المصادر (dorar/islamqa) اختيارية وموسومة صراحةً بأنها «تحتاج إنترنت» ولا يعتمد عليها عمل التطبيق.

## 7) Security Issues
- **XSS المخزَّن (C3):** أُصلح — كل مدخلات المستخدم عبر `textContent`/`escapeHtml`.
- لا `eval`، لا حقن سكربت ديناميكي، لا أسرار مضمّنة، لا موارد خارجية غير موثوقة.
- الروابط الخارجية تُفتح بـ`rel="noopener noreferrer"`.
- عامل الخدمة يخزّن ملفات محلية فقط (لا cache poisoning من مصدر خارجي).

## 8) UX Issues
واجهة Mobile First، أزرار ضخمة (≥48px) تُضغط بيد واحدة، RTL سليم، منع التمرير الأفقي، عدّاد واضح كبير، وضع ليلي بتباين جيّد، تلميح المرحلة الحالية، ورسائل تأكيد قبل إعادة العدّاد.

## 9) Performance Issues
- لا مكتبات خارجية، لا صور كبيرة (الأيقونة SVG صغيرة)، الحزمة كلها بضعة كيلوبايت.
- تُخزَّن الحالة عند التغيير فقط، والقوائم تُبنى مرة عند البحث. يفتح فوراً حتى على هاتف متوسط.

## 10) Remaining Unverified Items (تحتاج مراجعة بشرية)
1. **الرقم الدقيق لأثر «تقصير المرأة قدر أنملة»** — الحكم ثابت عند أهل العلم لكن نسبته لرقم حديثٍ بعينه تحتاج تدقيقاً من مختص؛ لذلك عُرض الحكم دون الجزم برقم رواية. (راجع `RELIGIOUS_AUDIT.md`).
2. **أرقام الأحاديث** الأخرى صحّتها راجحة لكنها لم تُدقَّق نسخةً بنسخة مع المطبوع؛ يُستحسن مراجعة مختص قبل النشر النهائي.
3. **المعلومات التنظيمية الرسمية** (تصاريح نسك ونحوها) لم تُدرَج عمداً كمحتوى ثابت لأنها متغيّرة وتحتاج تاريخ تحقق واضح.

---

## الخلاصة النهائية (إجابات أسئلتك)

- **ما الذي تغيّر؟** تسجيل عامل الخدمة، إصلاح XSS، تحصين الحالة ضد التلف، إعادة تصميم العدّادات (0→7 + حارس نقر + إعادة بتأكيد)، آلة حالة لطيفة، نموذج بيانات شرعي موحّد بإظهار الدرجة والخلاف، إكمال الشاشات الناقصة، وتحسينات PWA/UX/A11y.
- **لماذا تغيّر؟** لأن التطبيق لم يكن يعمل Offline فعلاً، وكان به ثغرة XSS وهشاشة في الحالة، والتزاماً بقاعدة «الدقة الشرعية قبل الشكل».
- **ما الذي اختُبر؟** 28 اختباراً آلياً في متصفح فعلي: التحميل، Offline (خادم موقوف + شبكة مقطوعة)، إعادة التحميل، ثبات الحالة، تلف التخزين، الطواف/السعي والاتجاه، التراجع/الإعادة، منع الزيادة المزدوجة، الوضع الليلي، البحث، رسائل الفراغ، وعدم تنفيذ حمولة XSS.
- **ما لم أستطع اختباره؟** التثبيت الفعلي كـPWA على جهاز حقيقي، وقارئات الشاشة الفعلية (روجعت السمات يدوياً)، ودقّة النطق العربي (يعتمد على أصوات المتصفح/النظام).
- **المحتوى الديني الذي يحتاج مراجعة بشرية:** البند 10 أعلاه — خاصةً رقم أثر «تقصير المرأة».
- **هل يعمل Offline بالكامل؟** نعم، تم التحقق فعلياً.
- **هل هناك اعتمادية تحتاج إنترنت؟** لا شيء لازم لعمل التطبيق. فقط روابط المصادر الاختيارية موسومة بأنها تحتاج إنترنت.
